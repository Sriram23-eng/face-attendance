# pip packages — unpinned for Python 3.13+ compatibility
requests
numpy
opencv-python
face-recognition
pyserial
adafruit-circuitpython-fingerprint
mfrc522
RPi.GPIO
RPLCD
smbus2

# NOTE: picamera2 is NOT installed via pip on Raspberry Pi.
# Install it via apt (it's pre-installed on Raspberry Pi OS Bookworm):
#   sudo apt install -y python3-picamera2 python3-libcamera
